"use client";

import { styled } from "styled-components";

const DeviceModalBackground = styled.div`
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.5);
  z-index: 10;
`;

export default function DevicesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <>
      <DeviceModalBackground />
      {children}
    </>
  );
}
